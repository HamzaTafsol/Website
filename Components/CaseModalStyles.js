import React from "react";
import { makeStyles } from "@mui/styles";
export const CaseModalStyles = makeStyles({
  caseMain: {
    "& .between-text-b": {
      display: "flex",
      justifyContent: "space-between",
    },
  },
});
